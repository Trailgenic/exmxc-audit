export const railwayClient = {
  postJob: async (url: string) => {
    // TODO: Implement POST /jobs
  },

  getJob: async (jobId: string) => {
    // TODO: Implement GET /jobs/:jobId
  },

  getEntity: async (entityId: string) => {
    // TODO: Implement GET /entities/:id (later)
  }
};

