{
  "ontologyVersion": "0.0.1",
  "constraints": [
    {
      "id": "CR1",
      "description": "Canonical root must match the normalized homepage root.",
      "test": {
        "type": "equality",
        "left": "canonicalRoot",
        "right": "identityRoot"
      }
    },
    {
      "id": "CR2",
      "description": "Organization.name must match the extracted entity name when Organization schema is present.",
      "test": {
        "type": "equality",
        "left": "orgSchemaName",
        "right": "entityName"
      }
    },
    {
      "id": "CR3",
      "description": "Breadcrumb root must resolve to the homepage canonical.",
      "test": {
        "type": "equality",
        "left": "breadcrumbRoot",
        "right": "canonicalRoot"
      }
    },
    {
      "id": "CR4",
      "description": "Multiple Organization schemas must not conflict on name.",
      "test": {
        "type": "boolean",
        "field": "orgSchemasAllHaveSameName",
        "expected": true
      }
    },
    {
      "id": "CR5",
      "description": "Canonical URLs must not represent multiple competing entity roots.",
      "test": {
        "type": "countEquals",
        "field": "canonicalRoots.count",
        "expected": 1
      }
    },
    {
      "id": "CR6",
      "description": "Entity root must not be undefined when schema objects indicate an Organization or Person.",
      "test": {
        "type": "notNull",
        "field": "entityName"
      }
    },
    {
      "id": "CR7",
      "description": "Schema.org vocabulary must be used for structural definition.",
      "test": {
        "type": "boolean",
        "field": "allSchemaUseSchemaOrg",
        "expected": true
      }
    },
    {
      "id": "CR8",
      "description": "If Person schema is present, it must not contradict the owning organization identity.",
      "test": {
        "type": "boolean",
        "field": "personSchemaDoesNotConflict",
        "expected": true
      }
    },
    {
      "id": "CR9",
      "description": "Breadcrumb path hierarchy must be structurally consistent (no logical jumps or unauthorized root substitutions).",
      "test": {
        "type": "boolean",
        "field": "breadcrumbHierarchyValid",
        "expected": true
      }
    },
    {
      "id": "CR10",
      "description": "Canonical URL must not contain unnecessary query parameters, tracking tags, or trailing variants.",
      "test": {
        "type": "boolean",
        "field": "canonicalIsClean",
        "expected": true
      }
    },
    {
      "id": "CR11",
      "description": "Top-level identity declaration (title root, canonical, organization.name) must not conflict across surfaces.",
      "test": {
        "type": "boolean",
        "field": "identityRootsUnified",
        "expected": true
      }
    },
    {
      "id": "CR12",
      "description": "Schema-detected identity must not conflict with the hostname-level entity root.",
      "test": {
        "type": "boolean",
        "field": "hostnameMatchesIdentityRoot",
        "expected": true
      }
    },
    {
      "id": "CR13",
      "description": "Homepage must carry at least minimal narrative depth for stable entity reconstruction.",
      "test": {
        "type": "minWordCount",
        "field": "wordCount",
        "min": 300
      }
    },
    {
      "id": "CR14",
      "description": "Homepage must expose at least one internal path to its own lattice (no pure dead-end shells).",
      "test": {
        "type": "boolean",
        "field": "hasInternalLinks",
        "expected": true
      }
    },
    {
      "id": "CR15",
      "description": "Entity must declare itself via Organization or Person schema to be treated as a primary EEI subject.",
      "test": {
        "type": "boolean",
        "field": "hasOrgOrPersonSchema",
        "expected": true
      }
    }
  ]
}
