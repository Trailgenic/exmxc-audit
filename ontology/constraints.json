[
  {
    "id": "C1_SINGLE_CANONICAL_DOMAIN",
    "description": "Canonical URL must not point to a different primary domain than the crawled URL.",
    "severity": 0.8,
    "domains": ["Entity", "Signal", "Schema"],
    "appliesTo": ["Canonical Clarity"],
    "logic": {
      "type": "canonical-domain-match"
    }
  },
  {
    "id": "C2_ORG_SCHEMA_DOMAIN_ALIGNMENT",
    "description": "Organization schema URL (or sameAs) should share the same root domain as the crawled URL.",
    "severity": 0.9,
    "domains": ["Entity", "Schema"],
    "appliesTo": ["Organization Schema"],
    "logic": {
      "type": "org-schema-domain-alignment"
    }
  },
  {
    "id": "C3_BREADCRUMB_ROOT_COHERENCE",
    "description": "Breadcrumb root item should align with the site’s primary entity or home URL.",
    "severity": 0.6,
    "domains": ["Entity", "Schema"],
    "appliesTo": ["Breadcrumb Schema"],
    "logic": {
      "type": "breadcrumb-root-consistency"
    }
  },
  {
    "id": "C4_SOCIAL_ENTITY_CONSISTENCY",
    "description": "Social links (sameAs) should not point to obviously conflicting brand identities.",
    "severity": 0.7,
    "domains": ["Entity", "Signal"],
    "appliesTo": ["Social Entity Links"],
    "logic": {
      "type": "social-entity-consistency"
    }
  },
  {
    "id": "C5_MINIMUM_STRUCTURAL_GROUNDING",
    "description": "Entity should expose at least one of: Organization schema, breadcrumb, or social entity links.",
    "severity": 0.5,
    "domains": ["Entity", "Schema", "Signal"],
    "appliesTo": [
      "Organization Schema",
      "Breadcrumb Schema",
      "Social Entity Links"
    ],
    "logic": {
      "type": "minimum-identity-signals",
      "minSignals": 1
    }
  }
]

